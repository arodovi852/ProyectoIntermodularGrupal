/**
 * Índice de DTOs
 * Exporta todos los Data Transfer Objects
 */

const UserDTO = require('./UserDTO');
const SongDTO = require('./SongDTO');
const PlaylistDTO = require('./PlaylistDTO');

module.exports = {
  UserDTO,
  SongDTO,
  PlaylistDTO
};

